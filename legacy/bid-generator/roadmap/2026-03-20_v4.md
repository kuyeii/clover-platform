# ProEngine Roadmap — 2026-03-20 v4
> 快照时间：2026-03-20 11:50（本地时间）
> 当前阶段：阶段 X — 多选 UI 统一化 + 按钮栏重整完成

## 本次新增（2026-03-20 v4）

### RequirementsReview — 多选 UI 统一化
- [x] TreeNode 删除 `multiSelectMode` 开关，checkbox 常态可见（仅叶子节点）
- [x] checkbox + 状态图标并排两个独立元素（checkbox 左 → 状态图标右）
- [x] 提取中节点：checkbox locked（opacity-40）+ 整行 opacity-70
- [x] 删除顶栏"多选"按钮、"全部重提取"按钮、"导出解析报告"按钮
- [x] 顶栏右侧：只保留"解析中/取消解析"状态 + **下一步：技术方案**（px-4 py-2 明显加大）
- [x] 底部浮动操作栏：触发条件改为 `selectedNodeIds.size > 0`（无需进入多选模式）
- [x] 浮动底栏内含："清空选择" + "全量重提取" + "重提取选中(N)"
- [x] 导出解析报告移到左栏解析框架底部（低存在感 text-gray-400 按钮）

### TemplateEditor — 按钮栏重整
- [x] 顶栏精简：删除"保存"、"导出文档"、"一键生成/选中生成"按钮
- [x] 顶栏右侧：时间戳浅色提示（`保存于 HH:MM` / `编辑中...`）+ **下一步：投标文件**（px-4 py-2）+ `⋯` 更多菜单
- [x] `⋯` 菜单整合：立即保存 + 导出 Word 文档 + ——分割线—— + 导出大纲配置 + 导入大纲配置
- [x] 自动保存改为 onChange debounce 1.5s（不再是 60s 定时器）
- [x] 生成控制区域移到目录栏底部：isGeneratingAll → 进度+取消；有勾选 → 生成选中(N)+全部；默认 → 一键生成全部
- [x] `isLocked` 已含 `isGeneratingAll`（全量生成时所有 checkbox 锁定）

## 遗留

- [ ] OutlineGenerator 顶栏精简（"配置"改图标按钮），优先级 P2，用户可在下次会话处理
