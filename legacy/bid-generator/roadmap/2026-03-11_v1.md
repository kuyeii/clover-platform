# ProEngine Roadmap — 2026-03-11 v1
> 快照时间：2026-03-11 09:12（本地时间）
> 当前阶段：阶段 8 — 大纲与各流程优化

## 任务目标
- [x] 修复 `generate_outline` 后端接口处理字符串元素导致的 `AttributeError`
- [x] 在项目工作流的各个生成环节（如 BlueprintGenerator, OutlineGenerator, 解析提取等）添加“重试”按钮，防止临时错误中断整个进程

## 待办事项
- [x] 修改 `routes.py` line 822 处理 `isinstance(s, str)` 逻辑
- [x] 检索所有 `Project` 目录下的阶段组件，给含有 `error` 或 API 请求环节添加 Retry 按钮
  - [x] OutlineGenerator.tsx (增加 `generate` 重试函数与按钮)
  - [x] BlueprintGenerator.tsx (已有 `handleGenerate` 重试功能)
  - [x] ProjectCreator.tsx (增加 `重新解析` 按钮)
  - [x] TemplateEditor.tsx (增加 `重试生成` 按钮状态)
- [x] 同步修改 Roadmap 存档
