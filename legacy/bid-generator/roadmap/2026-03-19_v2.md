# ProEngine Roadmap — 2026-03-19 v2
> 快照时间：2026-03-19 17:55（本地时间）
> 当前阶段：阶段 X — 重连后进度可感知优化

# Dify 连接中断问题 — 完整实施记录

## P0（已完成）：前端组件 mount 时主动重连
- [x] 三个组件均已有完整重连逻辑，无需修改

## P1（已完成）：后端 outline 任务真正可中断
- [x] `task_manager.py`：Task dataclass 增加 `dify_task_id` 字段，新增 `set_dify_task_id` 方法
- [x] `routes.py`：`_call_dify_workflow_stream` 每次 yield dict 携带 `dify_task_id`
- [x] `task_routes.py`：`start-outline` 注册 `dify_task_id`，`cancel_task` 增加 Dify Stop API 调用

## P2（已完成）：content 任务切换为哑流（Silent Streaming）
- [x] `start-content` 改用 `_call_dify_workflow_stream`，忽略 text_chunk，捕获 workflow_finished outputs
- [x] content 任务也支持 Stop API

## 补丁 1（已完成）：重连后队列可感知优化
- [x] `RequirementsReview.tsx`：重连前收集无内容叶子节点，设为 `extractingIds`，onNodeComplete 逐个移除
- [x] `projectService.ts`：新增 `resumeContentTask` 方法（首次立即检查，10s 间隔轮询）
- [x] `TemplateEditor.tsx`：刷新后对有 `task_id` 的 generating 章节恢复轮询，无 task_id 才置 error
- [x] `projectService.ts`：`generatedContent` 类型增加 `stage` 字段，修复 TS lint
