# ProEngine Roadmap — 2026-03-16 v8
> 快照时间：2026-03-16 11:37（本地时间）
> 当前阶段：综合审查与体验优化完成

# 投标文件模块重构

## P1 — 双列布局 ✅
- [x] `BidDocWorkbench.tsx` + `BidModule` 类型 + `App.tsx` 接入

## P2 — 智能填写 (Layer 1+2) ✅
- [x] Layer 1 结构化字段替换 + Layer 2 日期规则 + 按钮绑定

## P3 — 自评独立化 ✅
- [x] StageTopBar 5 阶段 + review case

## P5 — 解析报告重构 ✅
- [x] 三区布局（预设模板树 / 解析内容只读 / PDF 预览）
- [x] 框架纯后端化：`analysis_framework.json` + API
- [x] 逐节点 extractionPrompt 精细化（7 大类 14+ 叶子节点）
- [x] UI：只读内容、移除确认、导出按钮右上、重新生成按钮、空内容占位

## P4 — Dify BidDoc 工作流 ✅（已由 content_writer 覆盖）
- [x] Layer 3 LLM 补全 = 现有 `DIFY_WORKFLOW_CONTENT_WRITER` + `ContentEditor`

---

## P6 — 解析报告 SSE 生成 ✅

### 6.2 知识库上传
- [x] `KnowledgeManager` 扩展 + 固定 `bid_base` 知识库模式
- [x] SSE 端点双模式（KB / 全文回退）
- [x] 用户配置：Dify 工作流 + `.env`

### 6.3 Dify 解析工作流
- [x] 后端 SSE `POST /projects/analyze` + 前端 SSE 消费 + UI 绑定

### 6.4 生命周期
- [x] 文档自动清理（只删文档不删库）
- [ ] 提取结果 PIPT 占位符还原（低优先，按需开启）

---

## P7 — 下游联动 ✅
- [x] `form_toc` → `requiredAttachments` → BidDocWorkbench（解析编号列表→附件数组）
- [x] `eval_criteria` → `scoringCriteria`（解析 Markdown 表格→结构化评分数组）
- [x] `resp_substance` → `deviationTable`（解析条款→偏离表行，仅首次写入）
- [x] `form_format` → `formatRules`（保存排版规则字段）

---

## P8 — Dify 2.0 升级（延后，全流程跑通后再做）
- [ ] 升级 Dify（Docker Compose pull + up）
- [ ] 知识库切换父子模式（Parent-child-HQ）— 评分标准/废标项精度提升
- [ ] 评估 DOCX → Markdown 流水线（模板6）替代自有解析
- [ ] 评估 LLM 上下文增强（模板5）用于评分表格
- [ ] 在 Dify UI 搭简单 KB 检索工作流 → 导出 DSL → 批量生成 14 节点并行工作流
- [ ] 现有工作流兼容性验证
