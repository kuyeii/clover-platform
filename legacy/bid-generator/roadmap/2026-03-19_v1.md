# ProEngine Roadmap — 2026-03-19 v1
> 快照时间：2026-03-19 17:30（本地时间）
> 当前阶段：阶段 X — Dify 连接中断修复（P0/P1/P2 全部完成）

# Dify 连接中断问题 — 实施任务清单

## P0：前端组件 mount 时主动重连

- [x] 确认受影响的前端组件列表 — 已全部确认并加了重连逻辑（OutlineGenerator/RequirementsReview/TemplateEditor），无需修改

## P1：后端 — outline 任务真正可中断

- [x] `task_manager.py`：Task dataclass 增加 `dify_task_id: Optional[str]` 字段
- [x] `task_manager.py`：新增 `set_dify_task_id(task_id, dify_task_id)` 方法
- [x] `routes.py`：`_call_dify_workflow_stream` 在每次 yield dict 时携带 `dify_task_id`
- [x] `task_routes.py`：`start-outline` 的 `_run()` 提取并存入 `dify_task_id`
- [x] `task_routes.py`：`cancel_task` 端点新增 `_stop_dify_workflow()` 调用（仅对 streaming 任务）

## P2：后端 — content 任务切换为哑流（Silent Streaming）

- [x] `task_routes.py`：`start-content` 的 `_run()` 改用 `_call_dify_workflow_stream`
- [x] 哑流逻辑：忽略 `text_chunk` 事件，只捕获 `__finished__` 的完整 outputs
- [x] 顺带捕获 `dify_task_id`，让 content 任务也支持 Stop
- [x] 验证 think 标签清洗在完整字符串上正确工作
