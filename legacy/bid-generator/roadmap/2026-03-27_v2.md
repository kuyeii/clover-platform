# ProEngine Roadmap — 2026-03-27 v2
> 快照时间：2026-03-27 16:23（本地时间）
> 当前阶段：阶段 P4 — 富文本双向 Markdown 同步修复 

# Task: SVG 图表 / 图片引用 / 渲染全链路

## P0 — DOCX 导出 `<diagram>` SVG 处理
- [x] `gateway-out/src/forge.py`：在 `build()` 中插入 `_strip_diagrams_to_images()` 预处理，利用 `svglib` 将 SVG 转为临时 PNG，MD 替换后传入 MarkdownToDocxConverter

## P1 — imageMap 绝对路径 → 浏览器可访问 URL
- [x] `routes.py`：`_process_image` 输出改为 `{abs_path, preview_url, description}` 结构；新增 `/api/extracted-images/{filename}` 静态路由
- [x] `projectService.ts`：更新 `imageMap` 类型，`forgeDocument` 请求中只传 `abs_path`

## P2 — Content_Writer AI 引用 imageMap
- [x] `ProEngine_Content_Writer.yml`：Start 节点新增 `image_map_hint` 变量；两个 LLM 节点提示词追加图片引用规则
- [x] `task_routes.py`：`start_content_task` inputs 新增 `image_map_hint`
- [x] `projectService.ts`：`generateContentStream` 构建并透传 `imageMapHint`

## P3 — 插图画廊 UI
- [x] `TemplateEditor.tsx`：工具栏新增「附件图片」按鈕，展示 `imageMap` 缩略图画廊，点击 dispatch `proengine:insert-image` 事件
- [x] `ContentEditor.tsx`：监听 `proengine:insert-image` 事件，调用 TipTap `setImage` 插入光标位置

## P4 — 富文本双向 Markdown 同步修复 (WYSIWYG 修复)
- [x] `frontend-web`：安装 `turndown` 和 `turndown-plugin-gfm`
- [x] `ContentEditor.tsx`：引入 TurndownService 并配置 GFM 插件
- [x] `ContentEditor.tsx`：增加对 `<diagram>` 的安全反序列化规则
- [x] `ContentEditor.tsx`：实现 `toMarkdown`，在 `onUpdate` 和 `toggleSource` 中向外输出/渲染原生的 Markdown
- [x] `ContentEditor.tsx`：修复 SSE 外部 content 传入时光标乱跳的问题（校验比对 `lastEmittedMdRef`）

## 完成后操作
- [ ] 在 Dify 控制台重新导入 `ProEngine_Content_Writer.yml`（DSL 变更需手动导入生效）
- [x] 更新 roadmap 快照
