# ProEngine Roadmap — 2026-03-13 v1
> 快照时间：2026-03-13 09:28（本地时间）
> 当前阶段：阶段 1-5 完成 — 数据模型 + 后端接口 + PDF预览 + 技术方案入口弹窗

---

## 第一阶段：数据模型调整 [x]
- [x] 扩展 `ProjectStatus`（新增 10 个状态节点）
- [x] 新增 `AnalysisNode` / `TechProposalConfig` / `DeviationRow` 接口
- [x] 扩展 `Project` 接口（`analysisReport` / `pdfUrl` / `targetConfig` / `deviationTable`）
- [x] 后端 `schemas.py` 新增 `AnalysisNode` + `ExtractRequirementsResponse.analysis_report`

## 第二阶段：后端解析接口升级 [x]
- [x] 新增 `_convert_to_pdf_and_cache`（LibreOffice / docx2pdf 双方案）
- [x] `/projects/extract` 端点支持 DOC/DOCX 转 PDF，返回 `pdf_url` 和 `analysis_report`
- [x] `projectService.extractRequirements` 保存 `analysisReport` / `pdfUrl`，状态改为 `report_done`

## 第三阶段：前端侧边栏重组 [x]
- [x] `Sidebar.tsx` STATUS_CONFIG 和 DOT_COLOR 新增所有五阶段状态标签

## 第四阶段：技术方案前置弹窗 [x]
- [x] 新建 `TechProposalGate.tsx`，强制输入预期总字数/总页数，含快捷预设

## 第五阶段：PDF 预览面板 [x]
- [x] 新建 `PdfPreview.tsx`，右侧可折叠 `<iframe>` 渲染
- [x] `TemplateEditor.tsx` 接入 `pdfUrl` prop，四栏布局最右侧渲染 `PdfPreview`
- [x] `App.tsx` 新增五阶段路由（report_done、tech_proposal 等），传入 `pdfUrl`

## 第六阶段：响应表（偏离表）制作 [ ]
- [ ] 新建响应表页面，从解析报告"响应审查"节点自动预填条款
- [ ] 支持每行响应状态编辑（full / partial / deviate / na）

## 第七阶段：投标文件编排 [ ]
- [ ] 富文本编辑器（Word 风格）汇总技术方案 + 响应表 + 附件

## 第八阶段：导出 [ ]
- [ ] 基础导出按钮（Docx）
- [ ] 自定义导出配置（后续单独讨论）

---

## 验证
- [x] TypeScript 类型检查全部通过（无报错）
- [ ] 全流程端到端测试
