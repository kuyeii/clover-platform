# ProEngine Roadmap — 2026-03-20 v6
> 快照时间：2026-03-20 11:30（本地时间）
> 当前阶段：阶段 X — 下一步提升至外层 StageTopBar

## 本次新增（2026-03-20 v6）

### 下一步按钮提升至 StageTopBar（外层顶栏）
- [x] **StageTopBar.tsx** 完整重写：第一行项目名右侧加"下一步"按钮
  - 屏幕自适应：左侧项目名 `truncate min-w-0 flex-1`，右侧按钮 `shrink-0 whitespace-nowrap`
  - `activeTab` 动态决定按钮文字：analysis/outline→"下一步：技术方案"，tech→"下一步：投标文件"，bid→"完成：导出文件"，export→不显示
  - `isGenerating` 状态自动 disabled（同时 Tabs 行改 `overflow-x-auto` 防溢出）
- [x] **App.tsx** 更新
  - `go_bid` 分支补上 `status: 'deviation_editing'` 的状态更新（原在 TemplateEditor 按钮 onClick 里）
  - 从 RequirementsReview / TemplateEditor / BidDocWorkbench 的 JSX 删除 `onNextStep` prop 传递
  - RequirementsReview 的 `generating` prop 传递也同步删除
- [x] **RequirementsReview.tsx** 移除 `onNextStep` / `generating` Props 及函数参数
- [x] **TemplateEditor.tsx** 移除 `onNextStep` Prop 及函数参数
- [x] TSC 编译 0 错误

## 遗留
- [ ] OutlineGenerator 顶栏精简（"配置"改图标按钮），P2
