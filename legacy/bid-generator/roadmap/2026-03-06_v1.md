# ProEngine Roadmap — 2026-03-06 v1
> 快照时间：2026-03-06 11:15（本地时间）
> 当前阶段：阶段九 — 深度自动化、LLM 质量控制与体验优化

# ProEngine 阶段七 Gateway-Forge — 任务清单

## ✅ 阶段七核心任务

### gateway-out 模块扩展
- [x] `restorer.py`：扩展 `DEFAULT_PATTERN` 支持 `{{__BIDDER_*__}}`，新增 `restore_bidder()` 和 `restore_all()` 方法
- [x] `forge.py`（新文件）：`DocumentForge` 类，`build()` 方法执行五步流程：
  - [x] 步骤一：全量占位符还原（BIDDER 优先 + PIPT）
  - [x] 步骤二：各章节 Markdown 拼接
  - [x] 步骤三：`MarkdownToDocxConverter` 生成基础 `.docx`
  - [x] 步骤四：评分表嵌入（python-docx 内嵌表格 + 深蓝表头样式）
  - [x] 步骤五：附件逐份独页追加
- [x] `gateway-out/src/__init__.py`：导出 `DocumentForge`

### 后端路由新增
- [x] `routes.py`：`POST /projects/forge-document` 路由
  - [x] `_ForgeDocumentRequest` Schema（sections/scoring_rows/attachments/mapping_table/bidder_info）
  - [x] 动态导入 `DocumentForge`，调用 `build()` 返回 `.docx` 文件流

### 前端 Service 层
- [x] `projectService.ts`：新增 `forgeDocument()` 方法（`responseType: 'blob'`，触发下载）

### 前端 UI
- [x] `TemplateEditor.tsx`：工具栏新增「生成最终文档」按钮（emerald 绿色，有完成章节时可用）
- [x] `App.tsx`：修复 `generating_content`/`done` 状态渲染为 `TemplateEditor + ScoringTable + AttachmentFiller`
- [x] `App.tsx`：移除无用 `TaskOrchestrator` import

## ✅ 整体检查
- [x] `restorer.py` DEFAULT_PATTERN 同时匹配 PIPT + BIDDER
- [x] App.tsx generating_content 状态渲染已修正
- [x] TemplateEditor.tsx projectId 正确透传

## ✅ 阶段八：AI 附件动态生成与知识库看板
### 附件生成重构
- [x] `projectService.ts` / `schemas.py`：支持动态 `required_attachments` (包含 id, name, type, description)
- [x] `AttachmentFiller.tsx`：前端组件替换固定的 Jinja2 展示逻辑，改为根据 AI 提取的动态列表渲染
- [x] `routes.py`：`/projects/generate-attachment` 双轨制重构，动态附件触发 `DIFY_WORKFLOW_ATTACHMENT_GENERATOR`
- [x] `config.yaml` / `dify-workflows.md`：新增工作流配置说明

### 知识库面板联调
- [x] `routes.py` / `schemas.py`：新增 `GET /knowledge/documents` 从 Dify 获取数据集文档
- [x] `routes.py`：新增 `POST /knowledge/sync` 触发 `scripts/sync_kb.py`
- [x] `KnowledgeHub.tsx`：移除 Mock 数据，接入 API 并实现自动轮询刷新进度
- [x] `sync_kb.py`：强化解析能力，复用网关全格式解析器支持 `.pdf`/`.docx` 等后缀，加入防 OOM 的 4000 字符动态切块脱敏重组链路
- [x] `docx_parser.py` / `pdf_parser.py`：补全底层文件的表格文字提取链路，转换补齐为标准 Markdown 表格增强 LLM 供血

## 🚦 阶段九：深度自动化、LLM 质量控制与体验优化 (Next Phase)
### 1. Dify 节点审阅迭代 (In-Dify Review)
- [ ] 探索在 Dify 蓝图生成或章节详情工作流中引入 LLM 自动巡检/打分节点
- [ ] 构建 Self-Refine 的循环链路，对输出内容自动提建议并打回重写

### 2. 前端高阶自动化与防伪检测
- [ ] 新增“全局一键生成”按钮（覆盖大纲、正文、附件、自评表的串联调用）
- [ ] 在执行一键生成前，实施必要的配置项校验（卡点），并通过 Toast/弹窗提醒用户补充遗漏的“企业信息”等前置依赖

### 3. 提示词工程重构解耦
- [ ] 拆分混淆的提示词结构：隔离“技术大纲主体”与“商务附件生成”逻辑
- [ ] 重构 Prompt 使 LLM 的生成注意力更集中，降低商务与技术部分的干涉

### 4. 知识库多模态（图片）处理与 Docx 还原方案（核心重难点）
- [ ] **调研最优实践**：现阶段纯文本知识库/Dify 无法索引原图。需设计占位符方案（如 `{{IMG_ID}}`），让后端脱敏入库时记录图片在本地的绝对映射
- [ ] **Python 还原链路**：改造 `gateway-out`，在生成最终 Word 文档时，拦截图片占位符并利用 `python-docx` 重新插图
- [ ] **自评表页码联动**：探索在组装完 Word 后获取总页码/图表页码反填回自评表的可能性

### 5. 去“技术化”白标体验 (White-label UI)
- [ ] 移除前端文案中的 “Dify”、“API”、“脱敏”、“模型节点”等技术栈名词
- [ ] 弱化“文件上传成功”等涉及数据出域的用词感知，改为“导入分析”、“资料就绪”等更私有化的表述，增强用户对本地安全的信任感
