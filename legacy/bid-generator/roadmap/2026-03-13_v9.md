# ProEngine Roadmap — 2026-03-13 v9
> 快照时间：2026-03-13 14:38（本地时间）
> 当前阶段：P0 + P1 完成

---

## P0 Tiptap 富文本编辑器 ✅

### 新增文件
- `ContentEditor.tsx` — 基于 @tiptap/react 的所见即所得编辑器
  - 工具栏：标题/加粗/斜体/下划线/删除线/高亮/列表/引用/代码块/表格/撤销重做
  - Markdown→HTML 自动转换（marked 库）
  - 源码模式切换
  - readOnly 属性控制

### 修改文件
- `TemplateEditor.tsx` — ReactMarkdown → ContentEditor（done 可编辑 / generating 只读）
- 新增 `handleContentEdit` 回调同步 contentStates 和字数

---

## P1 SQLite 数据库部署 ✅

### 后端
- `database.py` — 新增 `ProjectRecord` 模型（宽表 JSON 存储）
- `project_routes.py` — 完整 CRUD API（GET/POST/PUT/DELETE + batch 批量导入）
- `main_lite.py` — 注册 project_router 到 /api 前缀

### 前端
- `projectService.ts` — 新增同步层：
  - `syncProjectToServer()` — 后台推送单个项目
  - `deleteProjectFromServer()` — 后台删除
  - `syncFromServer()` — 服务器拉取合并
  - `migrateToServer()` — 批量迁移
  - create/update/delete 自动触发同步
- `App.tsx` — 启动时 migrateToServer → syncFromServer → refreshProjects
