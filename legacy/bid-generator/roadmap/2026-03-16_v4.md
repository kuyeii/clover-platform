# ProEngine Roadmap — 2026-03-16 v4
> 快照时间：2026-03-16 10:36（本地时间）
> 当前阶段：P6 — Dify RAG 解析生成（规划中）

# 投标文件模块重构

## P1 — 双列布局 ✅
- [x] `BidDocWorkbench.tsx` + `BidModule` 类型 + `App.tsx` 接入

## P2 — 智能填写 (Layer 1+2) ✅
- [x] Layer 1 结构化字段替换 + Layer 2 日期规则 + 按钮绑定

## P3 — 自评独立化 ✅
- [x] StageTopBar 5 阶段 + review case

## P5 — 解析报告重构 ✅
- [x] 三区布局（预设模板树 / 解析内容只读 / PDF 预览）
- [x] 框架纯后端化：`analysis_framework.json` + `/config/analysis-framework` API
- [x] 前端零硬编码
- [x] 逐节点 extractionPrompt 精细化（7 大类 14+ 叶子节点）
- [x] systemPrompt（6 条核心原则）

## P4 — Dify BidDoc 工作流（待实施）
- [ ] `ProEngine_BidDoc_Filler` 工作流（Layer 3 LLM 补全）
- [ ] 后端 API `/projects/fill-bid-module`

---

## P6 — Dify 知识库 RAG 解析生成（TODO）

> 当前 Dify 版本：1.13.0（Docker Compose），已支持知识库 + 工作流 + RAG

### 6.1 知识库架构
- [ ] 通用知识库（长期）：公司资质/业绩/模板（用于智能填写）
- [ ] 项目招标文件知识库（按项目隔离）：`project_{id}_bid_doc`
- [ ] `KnowledgeManager` 扩展：增加 `create_dataset()` + 动态 `dataset_id` 支持

### 6.2 PIPT → 知识库上传链路
- [ ] PDF → 文本解析（PyMuPDF）
- [ ] PIPT 脱敏（实体识别 → 占位符替换）
- [ ] 创建项目专属知识库 `POST /datasets`
- [ ] 上传脱敏文本 `POST /datasets/{id}/document/create_by_text`
- [ ] 等待索引完成（轮询 indexing_status）
- [ ] 前端：PIPT 阶段进度展示（暂不实装，先自动跳转）

### 6.3 Dify 解析工作流
- [ ] 新建 `ProEngine_DocAnalysis` 工作流 DSL
- [ ] 工作流输入：`node_id` + `extractionPrompt` + `dataset_id`
- [ ] 工作流逻辑：知识库检索（top_k=10-15）→ systemPrompt + 检索结果 → LLM → 输出 content
- [ ] 后端 `/projects/{id}/analyze` SSE 端点：逐节点调用工作流
- [ ] 前端 SSE 消费：左树节点逐个从灰变蓝，中面板实时填充

### 6.4 PIPT 还原（可选）
- [ ] 提取结果中的占位符 → PIPT 映射表还原原文（展示用）

### 6.5 生命周期管理
- [ ] 项目关闭 → 清理项目知识库（`delete_document` / `delete_dataset`）

---

## P7 — 提取结果下游联动（TODO）

### 投标文件目录 → 投标文件模块
- [ ] 解析 `form_toc` 编号列表 → `requiredAttachments` → BidDocWorkbench 自动初始化

### 评审标准 → 自评打分表
- [ ] 解析 `eval_criteria` Markdown 表格 → `[{项目, 标准, 分值}]` → 自评表

### 评审标准 → 技术方案生成权重
- [ ] 高分项(≥10分)分配更多篇幅，权重系数配置化

### 实质性条款 → 偏离表
- [ ] `resp_substance` → 偏离表条款来源，标注「不可偏离」

### 暗标格式 → 排版校验
- [ ] `form_format` → 投标文件生成时自动应用排版规则
