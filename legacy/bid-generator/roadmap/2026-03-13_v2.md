# ProEngine Roadmap — 2026-03-13 v2
> 快照时间：2026-03-13 09:55（本地时间）
> 当前阶段：阶段 1-7 全部完成 — 完整五阶段工作流 + PDF预览 + 响应表 + 投标文件编排

---

## 第一阶段：数据模型调整 [x]
- [x] 扩展 `ProjectStatus`（+10状态）+ `AnalysisNode`/`TechProposalConfig`/`DeviationRow` 三接口
- [x] `Project` 接口新增 `analysisReport`/`pdfUrl`/`targetConfig`/`deviationTable`
- [x] 后端 `schemas.py` 新增 `AnalysisNode` + `ExtractRequirementsResponse.analysis_report`

## 第二阶段：后端解析接口升级 [x]
- [x] `_convert_to_pdf_and_cache`（LibreOffice headless / docx2pdf 双方案）
- [x] `/projects/extract` 支持 DOC/DOCX/PDF 三格式，返回 `pdf_url` + `analysis_report`
- [x] `extractRequirements` 前端方法保存 `analysisReport`/`pdfUrl`，状态迁移至 `report_done`

## 第三阶段：前端侧边栏重组 [x]
- [x] `Sidebar.tsx` 全部18个状态标签 + 动画脉冲颜色

## 第四阶段：技术方案前置弹窗 [x]
- [x] `TechProposalGate.tsx`（字数/页数快捷预设 + 自定义输入，紫色渐变设计）
- [x] `App.tsx` 全状态路由完成，`pdfUrl` 传入 `TemplateEditor`

## 第五阶段：PDF 预览面板 [x]
- [x] `PdfPreview.tsx`（可折叠暗色面板，缩放控件，空/加载/错误三态）
- [x] `TemplateEditor.tsx` 四栏布局最右侧接入 `PdfPreview`

## 第六阶段：响应表（偏离表）制作 [x]
- [x] `projectService`：`saveDeviationTable` / `getDeviationTable` / `initDeviationTableFromReport`
- [x] `DeviationTable.tsx`：从解析报告自动预填、完全/部分/偏离/N-A 四档状态、统计角标、hover删除、自动保存
- [x] `App.tsx` `deviation_editing`/`deviation_done` → `DeviationTable`

## 第七阶段：投标文件编排 [x]
- [x] `BidDocAssembler.tsx`：技术方案+响应表+附件+封面+目录可视化列表，眼睛图标启/禁模块，展开预览内容，生成 docx
- [x] `App.tsx` `bid_assembling`/`bid_done`/`exporting`/`done` → `BidDocAssembler`

---

## 待后续讨论
- [ ] 导出自定义配置（第八阶段）
- [ ] Sidebar 五阶段线性锁定导航
- [ ] `TechProposalGate` 弹窗触发节点（RequirementsReview 确认后）
- [ ] `BidDocAssembler` 拖拽排序（已有拖拽把手占位）
