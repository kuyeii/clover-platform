# ProEngine Roadmap — 2026-03-05 v1
> 快照时间：2026-03-05 16:24（本地时间）
> 当前阶段：阶段七完成 — Gateway-Forge Docx 渲染与集成

---
# ProEngine 功能规划 TODO

## ✅ 已完成

- [x] 后端：`/projects/extract` — 文件解析 + 脱敏 + Dify 需求提取
- [x] 后端：`/projects/generate-outline` — 生成一级+二级大纲 + writingHint
- [x] 前端：ProjectCreator 接真实 API
- [x] 前端：OutlineGenerator 展示两级大纲 + writingHint 灯泡展开
- [x] 前端：Sidebar 脱敏设置面板 + Dify 工作流状态面板
- [x] 配置：`.env` 统一工作流密钥管理，`config.yaml` 只存开关
- [x] README：所有子工作区文档更新

---

## ✅ 阶段一：打通内容生成链路

- [x] 后端：`/projects/generate-content` 路由，转发 section 信息到 Dify `content_writer`
- [x] 前端：`projectService.generateContent()`，`TemplateEditor` 接入生成按钮 + 预览
- [x] 前端：每章节独立状态（idle / generating / done / error）

---

## ✅ 阶段二：脱敏占位符透明化

- [x] 后端：`PIPT mapping_table` 从 `/projects/extract` 响应中返回
- [x] 前端：`mapping_table` 存入 `Project.mappingTable`（localStorage）
- [x] 前端：`generateContent` 自动构建 `placeholder_hint` 注入 Dify
- [x] Dify：`content_writer` 开始节点需加 `placeholder_hint` 变量，System Prompt 末尾引用 `{{placeholder_hint}}`

---

## ✅ 阶段三：投标人信息配置

- [x] 前端：Sidebar 投标人信息面板（per-project，localStorage-only，已配置/未配置徽章）
- [x] 前端：`Project.bidderInfo` + `projectService.updateBidderInfo()`
- [x] 前端：`generateContent` 自动将投标人信息转为 `{{__BIDDER_*__}}` 占位符注入 `placeholder_hint`
- [ ] **gateway-out**（待做）：生成 .docx 时将 `{{__BIDDER_*__}}` 替换为本地真实值 ✅（已在阶段七实现）

---

## ✅ 阶段四：附件填写

- [x] 后端：`attachment_templates.py` 4 种 Jinja2 模板（申请书/委托书/无违规声明/廉洁承诺）
- [x] 后端：`/projects/generate-attachment` 路由，接收投标人信息渲染模板
- [x] 前端：`AttachmentFiller.tsx`，投标人信息自动预填，支持预览/复制/下载
- [x] 前端：`Project.requiredAttachments` 字段，Dify 返回时自动过滤展示

> [!IMPORTANT]
> **TODO（后续优先改进）**：`requirement_extractor` Dify 工作流的输出 Schema 中需增加
> `required_attachments` 数组，让系统从招标文件中动态识别要求的附件类型，
> 而不是依赖默认的 4 种。若招标文件无对应附件，则 fallback 到内置模板。

---

## ✅ 阶段五：自评评分表

- [x] 后端：`/projects/build-scoring-table` — 从 score requirements 初始化行结构
- [x] 后端：`/projects/fill-scoring-row` — AI 自动填写单行（策略：尽量响应/部分响应，禁止不响应）
- [x] 后端：`/projects/export-scoring-table` — openpyxl 导出 Excel
- [x] 前端：`ScoringTable.tsx` — 构建/逐行编辑/AI 单行或全表填写/Excel 导出
- [x] 前端：证明材料以文件路径占位符展示，gateway-forge 阶段注入真实文件

> [!IMPORTANT]
> **TODO — scoring_assistant Dify 工作流**：当前 AI 填写借用 `requirement_extractor` 密钥，
> 输出质量受工作流固定提示词影响。建议后续单独建立 `ProEngine_Scoring_Assistant` Chat 类工作流。

---

## ✅ 阶段六：全局蓝图（Blueprint）

> 在大纲确认后、内容生成前，生成全局投标策略文档，保证全文一致性

- [x] **Dify**：设计 `ProEngine_Blueprint_Generator` 工作流（需在 `.env` / `config.yaml` 补充密钥）
  - 输入：requirements / bid_type / outline（章节列表）
  - 输出：项目定位句、投标策略、差异化亮点 2-3 条、写作风格
- [x] **后端**：新增 `/projects/generate-blueprint` 路由
- [x] **前端**：App 引流新状态 `generating_blueprint` 和 `BlueprintGenerator.tsx` 组件
- [x] **前端**：蓝图可轻量编辑后确认
- [x] **后端/前端**：`projectService.generateContent` 调用时将 `project.blueprint` 格式化后注入

---

## ✅ 阶段七：Gateway-Forge (Docx 渲染与集成)

> 最终集成阶段，将前面所有的零散元素（内容、评分表、附件、脱敏还原）组合为最终文档

- [x] **`gateway-out/restorer.py`**：扩展支持 `{{__BIDDER_*__}}` 占位符，新增 `restore_bidder()` / `restore_all()`
- [x] **`gateway-out/forge.py`**（新建）：`DocumentForge` 类，五步组装流程
  - [x] 全量占位符还原（BIDDER → PIPT）
  - [x] 各章节 Markdown 拼接为文档主体
  - [x] `MarkdownToDocxConverter` 生成 `.docx`
  - [x] 评分表嵌入：Word 内嵌6列表格（深蓝表头，合计行）
  - [x] 附件追加：每份附件分页独立追加
- [x] **后端**：`POST /projects/forge-document` 路由，返回 `.docx` 文件流
- [x] **前端**：`projectService.forgeDocument()` 方法，`responseType: 'blob'`，触发下载
- [x] **前端**：`TemplateEditor.tsx` 工具栏新增「生成最终文档」绿色按钮
- [x] **前端 Bug Fix**：`App.tsx` `generating_content`/`done` 状态渲染从旧 `TaskOrchestrator` 改为 `TemplateEditor + ScoringTable + AttachmentFiller`

---

## 🔮 后续待办（未进入正式阶段）

- [ ] Dify 工作流：`ProEngine_Scoring_Assistant` 专属 Chat 工作流（提示词侧重论证评分响应）
- [ ] 接口扩展：`requirement_extractor` 输出 `required_attachments` 数组（动态识别招标文件附件要求）
- [ ] 接口扩展：`requirement_extractor` 输出 `scoring_table_template`（动态提取评分表结构）
- [ ] 前端扩展：`forgeDocument()` 支持传入 `attachments` 内容（当前为空数组占位）
- [ ] Word 整合：`export-scoring-table` 增加返回 Word 表格版本
