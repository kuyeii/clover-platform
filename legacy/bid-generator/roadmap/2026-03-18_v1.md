# ProEngine Roadmap — 2026-03-18 v1
> 快照时间：2026-03-18 13:10（本地时间）
> 当前阶段：阶段 8 — PIPT 敏感识别策略升级 & 图片处理优化

## 已完成

### 阶段 8：PIPT 优化（本次）

#### 8.1 PIPT 敏感识别策略升级
- [x] 新增 `PIPT_LLM_MODE` 配置项（`verify_only` / `augment` / `full`）
- [x] `augment` 三阶段 LLM 流程：Phase1 校验 → Phase2 主动挖掘 → Phase3 二次核验
- [x] `full` 模式：直接 LLM 端到端识别，跳过 HanLP NER
- [x] 统一 `_call_llm` 封装，消除重复代码
- [x] 新增 `PIPT_LLM_NER_RELAXED` 配置项：放宽 org 类型后缀/长度过滤
- [x] `_parse_llm_json` 鲁棒 JSON 解析（兼容 markdown 包裹）
- [x] `_verify_extra_entities`：专为新挖掘实体设计的保守核验，防止 2b 小模型幻觉

#### 8.2 VLM 图片打标可配置化
- [x] `max_tokens` → `VLM_MAX_TOKENS`（默认 200）
- [x] 超时 → `VLM_TIMEOUT`（默认 120s）
- [x] 并发数 → `VLM_CONCURRENT`（默认 2）
- [x] 尺寸过滤 → `VLM_MIN_IMAGE_SIZE_KB`（默认 5KB）
- [x] 失败自动重试（最多 2 次）

#### 8.3 知识库同步异步化
- [x] `sync_kb.py` 支持通过 `KB_SYNC_JOB_ID` + `KB_SYNC_STATUS_DIR` 写入进度状态
- [x] 新增 `POST /kb/sync`：后台触发，立即返回 job_id
- [x] 新增 `GET /kb/sync-status/{job_id}`：实时进度查询
- [x] 新增 `GET /kb/sync-jobs`：历史任务列表

#### 8.4 配置更新
- [x] `.env` 完整补充所有新增配置项及注释

## 历史阶段（略）

---

## v1 更新: EntityRegistry 全局规范化占位符

- [x] `database.py` 新增 `EntityRegistry` 全局实体注册表
- [x] `database.py` 新增 `FernetEncryptor` 字段加密工具（AES-128）
- [x] `engine.py` `desensitize()` 接入 EntityRegistry，跨文件复用占位符
- [x] `routes.py` `/restore` 升级：优先查全局表，回退 MappingRecord 兼容旧数据
- [x] `routes.py` `forge_document` 内部自动还原 PIPT 占位符，无需前端传 mapping_table
- [x] `.env` 新增 `PIPT_DB_KEY` Fernet 密钥配置
