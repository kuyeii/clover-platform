# ProEngine Roadmap — 2026-03-27 v1
> 快照时间：2026-03-27 15:35（本地时间）
> 当前阶段：阶段 3 — SVG 图表/图片渲染全链路

---

## P0 — DOCX 导出 `<diagram>` SVG 处理 ✅
- `gateway-out/src/forge.py`：新增 `_svg_to_temp_png`（svglib，md5 缓存）和 `_strip_diagrams_to_images`
- 在 `DocumentForge.build()` 中 `restorer.restore_all` 之后、`md_to_docx` 之前执行预处理
- 同步兼容旧 `image_map` 字符串值与新 `{abs_path, preview_url}` 字典值

## P1 — imageMap 双路径结构 ✅
- `routes.py` `_process_image`：返回 `{abs_path, preview_url, description}` 结构
- `routes.py`：新增 `/api/extracted-images/{filename}` 静态图片路由（含路径安全过滤）
- `projectService.ts`：`imageMap` 类型更新；`forgeDocument` 仅传 `abs_path`

## P2 — Content_Writer AI 引用 imageMap ✅
- `ProEngine_Content_Writer.yml`：Start 节点新增 `image_map_hint` 变量；两个 LLM 节点 System Prompt 追加【原始文档图片引用规则】（宁缺毋滥策略）
- `task_routes.py` `start_content_task.inputs`：新增 `image_map_hint`
- `projectService.ts` `generateContentStream`：构建并透传 `imageMapHint`（只含描述，不含服务器路径）
- **⚠️ 操作项**：需在 Dify 控制台重新导入 `ProEngine_Content_Writer.yml`

## P3 — 插图画廊 UI ✅
- `TemplateEditor.tsx`：工具栏新增「附件图片」按钮（有图片时才显示）+ 悬浮缩略图画廊
- `ContentEditor.tsx`：监听 `proengine:insert-image` 自定义事件，调用 TipTap `setImage` 插入当前焦点光标位置

---

## 历史遗留 Warnings（非本次引入）
- `kwEditTarget`/`setKwEditTarget`/`kwInput`/`setKwInput`/`mappingsLoading`/`handleToggleOriginal`/`updateActiveBlock` 已声明未使用 — 属于 TemplateEditor 旧代码遗留，待后续统一清理
