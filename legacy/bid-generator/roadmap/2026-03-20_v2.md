# ProEngine Roadmap — 2026-03-20 v2
> 快照时间：2026-03-20 10:15（本地时间）
> 当前阶段：阶段 X — 重试机制 + 错误 UX + 取消中断全链路完成

## 本次完成（2026-03-20）

### 重试机制
- [x] `routes.py`：`_call_dify_workflow` blocking 模式加重试（2次，间隔 5s）
- [x] `task_routes.py`：analyze `extract_group` 分组级别重试（2次，间隔 3s，JSON解析失败也重试）
- [x] `TemplateEditor.tsx`：前端软件层重试 —— onError 首次触发时自动重新调用 `handleGenerateContent`，stage 改为"⏳ 自动重试中..."

### 取消中断真实发送
- [x] `RequirementsReview.tsx`：取消按钮新增调用 `POST /tasks/{id}/cancel` 真实中断后端+Dify
- [x] `OutlineGenerator.tsx`：已有 cancel API，改为灰色样式
- [x] `TemplateEditor.tsx`：已有 cancel API，两处取消（全部取消/单章节取消）改为 `cancelled` 状态

### 取消 UX（灰色，不用红色）
- [x] `RequirementsReview`：取消按钮改为灰色，进度文案清空（不再显示"已手动取消"红色文字）
- [x] `OutlineGenerator`：新增 `isCancelled` state，显示灰色"【已取消】大纲生成已中断"
- [x] `TemplateEditor`：章节目录图标—cancelled=灰色 XCircle；副文字="【已取消】"（无红色）

### 错误 UX（橙色，非红色，三按钮）
- [x] `OutlineGenerator`：error 区域改为橙色"运行异常"+"重试"+"复制错误信息"+"报告问题（占位符）"
- [x] `TemplateEditor`：内容区 error 改为橙色"运行异常，请稍后重试"，同样三按钮

### 类型系统
- [x] `TemplateEditor.tsx`：`BlockContentState.status` 加 `'cancelled'`
- [x] `projectService.ts`：`generatedContent.status` 同步加 `'cancelled'`
- [x] `TemplateEditor.tsx`：补充 `XCircle` 导入

## 前期完成记录（2026-03-19）
- [x] Dify 重连、Silent Streaming、断流重试等所有 P0-P2 任务（详见 v1.md、v2.md）
- [x] 全链路审计修复：竞态阻断、404 静默化、OutlineGenerator catch 静默化
