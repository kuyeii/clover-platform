# ProEngine Roadmap — 2026-03-13 v5
> 快照时间：2026-03-13 11:20（本地时间）
> 当前阶段：SSE 流式生成 + 节点级进度完成

---

## 本轮改动总结

### Dify DSL 全面改造（6 个工作流）
- 全部关闭 structured_output，改用 LLM + Code 节点解析
- 占位符自动修复 Code 节点
- 模型统一为 DeepSeek-V3.2
- 新建 blueprint_generator

### SSE 流式生成 + 节点级进度

#### 后端（routes.py）
- `_call_dify_workflow_stream` 增加 `node_started` 事件解析
- 节点标题映射为用户友好的阶段标签（20+ 映射项）
- 循环轮次追踪（审查/润色显示 "(1/3)"）
- `event_stream` 推送三种事件：`text`/`stage`/`done`

#### 前端（projectService.ts + TemplateEditor.tsx）
- 新增 `generateContentStream` 方法（fetch + ReadableStream）
- `handleGenerateContent` 从 Blocking 改为流式
- 生成中 UI 改造：
  - 顶部渐变进度条 + 阶段标签（如 "✍️ 正文生成"）
  - 实时字数计数器
  - Markdown 打字机效果（内容实时追加渲染）
  - 无内容时显示"正在连接智能引擎..."

#### 后端适配（新 DSL 输出格式）
- scoring_assistant：优先直接读取拆分字段 → 降级 JSON 解析
- blueprint_generator：优先直接读取拆分字段 → 降级 JSON 解析
