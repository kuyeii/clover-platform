# ProEngine Roadmap — 2026-03-12 v1
> 快照时间：2026-03-12 09:35（本地时间）
> 当前阶段：阶段 8.2 — 修复投标人占位符提示词与还原兼容性

## 任务目标
在生成内容时，AI 会在每个段落末尾强行插入投标人信息占位符，且格式可能与还原器预期不符（简化版无双下划线），导致 Word 文档中出现未还原的裸占位符。

## 完成事项
- [x] 分析 `projectService.ts` 中 `placeholderHint` 的构造逻辑
- [x] 确认 `gateway-out/src/restorer.py` 的正则匹配现状
- [x] 修改 `projectService.ts`：
  - 优化 `bidderHint` 措辞：明确"仅在自然提及时使用"，禁止在总结语、段末强行插入
  - 强调双下划线格式，并示例说明 `{{__BIDDER_ORG__}}` vs `{{BIDDER_ORG}}`
  - 升级 `piitHint` 同样注明格式严格性
- [x] 修改 `gateway-out/src/restorer.py`：
  - 新增 `BIDDER_FIELD_MAP_FALLBACK` 兜底映射（无双下划线格式）
  - `restore_bidder` 方法改为两轮遍历：第一轮标准格式，第二轮兜底简化格式
- [x] 生成 Dify 改动指南：`dify_content_writer_prompt_guide.md`

## Dify 侧待操作（用户手动完成）
- [ ] 在 `content_writer` 工作流的 LLM 主写作节点 System Prompt 末尾追加占位符引用铁律
- [ ] 确认 `placeholder_hint` 变量已接入 LLM 节点的 User Message 模板
