# ProEngine Roadmap — 2026-03-10 v3
> 快照时间：2026-03-10 15:46
> 当前阶段：阶段十 — 待定与架构修补
## 🟢 优先级一（复杂度低，我这边可直接完成）

### 5. 去"技术化"白标体验 (White-label UI)
- [x] 扫描前端所有 .tsx 文件中的技术词汇
- [x] 替换映射：脱敏→隐私保护、Dify→智能引擎、解析中→智能分析中、服务正常运行中→智能服务已就绪
- [x] 修改文件：Sidebar.tsx / ProjectCreator.tsx / OutlineGenerator.tsx / TemplateEditor.tsx

### 2. 前端高阶自动化（"全局一键生成"）
- [x] `projectService.ts`：新增 `generateAll()` 串联方法
- [x] `TemplateEditor.tsx`：新增"一键生成全部"按钮 + 进度弹窗
- [x] 执行前卡点校验：投标人信息、Toast 提示缺项

## 🟡 优先级二（复杂度中，需要用户配合 Dify 配置）

### 3. 提示词工程重构解耦
- [x] 分析当前 `routes.py` 中内容生成与附件生成的 Dify 调用逻辑
- [x] 拆分并注入 `decoupling_instruction`：拦截大纲与正文生成过程中的商务附件污染
- [x] 用户需在 Dify 中对应的工作流补充承接上述变量，或直接更新 System Prompt

### 1. Dify 节点审阅迭代 (In-Dify Review)
- [x] 设计 Self-Refine 循环链路方案（结合现有 Dify 截图得出结论）
- [ ] **需用户在 Dify 中配置** LLM 巡检/打分节点
- [x] 后端对应调整：已新增 `quality_score` 字段解析和流转
- [x] 前端对应调整：已在 `TemplateEditor.tsx` 左侧章节列表及主卡片中实现质量分徽章和评审建议的展示。

## 🔴 优先级三（复杂度高，核心重难点）

### 4. 知识库多模态（图片处理、VLM 打标与 Docx 还原）
- [x] 调研并设计图片图片占位符与描述嵌入方案（`[图片内容说明：xxx] {{IMG_ID}}`）
- [x] 改造 `pipt-flask/app/api_lite/routes.py` 等底层链路：使用 `pymupdf4llm` 真正实现截图和图片导出，并增加与局域网内的通用 VLM 进行推理调用的打标客户端壳代码。
- [x] 改造 `gateway-out/forge.py`：拦截图片占位符，用 python-docx 插图还原（已支持对齐格式）
- [x] **自评表页码联动**：基于 `PAGEREF` 和 `<w:bookmark>` 实现底层跨章节 XML 原生域引用。

### 6. 基于本地 LLM 的 PIPT 实体二次校验
- [x] 更新 `.env` 补充 PIPT_LLM_VERIFY 等配置参数。
- [x] 改造 `pipt-flask/app/api_lite/engine.py` 中的 `recognize` 方法：将提取出的实体附带上下文发送至 LLM 进行正确性验证，并根据返回进行修正与精简。

## 🔮 待办池 (TODO / Phase 10)

### 7. 多模态上下文实体孪生 (Entity Context Twinning)
- [ ] **痛点**：当前 VLM 打标图片时独立运行，无法得知正文已抽取的 PIPT 实体，导致同一机构在图中被称为"某机构"，在正文中被称为 `{{ORG_1}}`，还原时无法统一。
- [ ] **方案**：重构 `routes.py` 时序，先通过 `engine.py` 抽出实体词典（`mapping_table`），再将其精简后（如仅包含 `ORG/NAME` 取前20）通过 Hint 注入给 VLM 的 System Prompt。
- [ ] **目标**：VLM 在描述图片时直接输出 `{{ORG_1}}` 等占位符，实现图文物理隔离下的上下文逻辑统一。
