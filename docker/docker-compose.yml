# Current development can use the external PostgreSQL 18 instance configured by .env.
# If local PostgreSQL is needed, add a local-only compose service in a later phase or
# in an uncommitted override file. Do not commit real production passwords here.
